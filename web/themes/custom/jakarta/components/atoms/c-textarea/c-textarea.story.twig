{# c-textarea.story.twig #}
{# for story purposes only #}
{% set attributes = create_attribute() %}
{% do attributes.setAttribute('type', 'textarea') %}
{# for story purposes only #}

{{ include('jakarta:c-textarea') }}
