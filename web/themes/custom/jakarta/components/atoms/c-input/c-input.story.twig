{# c-input.story.twig #}
{# for story purposes only #}
{% set attributes = create_attribute() %}
{% do attributes.setAttribute('type', 'text') %}
{# for story purposes only #}

{{ include('jakarta:c-input') }}
